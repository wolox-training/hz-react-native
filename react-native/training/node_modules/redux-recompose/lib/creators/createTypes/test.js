'use strict';

var _ = require('.');

var _2 = _interopRequireDefault(_);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('createTypes', function () {
  it('Creates an empty types object from an empty array', function () {
    var types = (0, _2.default)([], '@@NAMESPACE');
    expect(types).toEqual({});
  });
  it('Creates types object based on a string array', function () {
    var types = (0, _2.default)(['ONE', 'TWO'], '@@NAMESPACE');
    expect(types).toEqual({
      ONE: '@@NAMESPACE/ONE',
      TWO: '@@NAMESPACE/TWO'
    });
  });
  it('It throws if does not receive an array', function () {
    expect(function () {
      return (0, _2.default)({}, 'none');
    }).toThrow(Error, 'Action names must be an array of strings.');
  });
  it('It throws if does not receive a string array', function () {
    expect(function () {
      return (0, _2.default)(['s', {}], 'none');
    }).toThrow(Error, 'Action names must be an array of strings.');
  });
});