import { StyleSheet } from 'react-native';

import { newYorkPink } from '../../../../../../../constants/colors';

export default StyleSheet.create({
  textButton: {
    color: newYorkPink,
    fontSize: 13
  }
});
